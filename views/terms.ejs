<%- include('partials/header') %>
<main class="legal-page">
  <article class="legal-card">
    <h1><%= copy.terms.title %></h1>
    <p><%= copy.terms.intro %></p>
    <% copy.terms.sections.forEach((section) => { %>
      <h2><%= section.title %></h2>
      <p><%= section.body %></p>
    <% }) %>
  </article>
</main>
<%- include('partials/footer') %>
